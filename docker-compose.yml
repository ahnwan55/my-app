services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    env_file:
      - docker-compose.env
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/findb
      - AI_SERVER_URL=http://ai-server:8000
    depends_on:
      - postgres