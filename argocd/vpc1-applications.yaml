apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: vpc1-bundle
  namespace: argocd
spec:
  generators:
  - list:
      elements:
        - name: vpc1-ai-helm
          path: charts/ai
          values: values-vpc1.yaml
          namespace: bookjjeok
        - name: vpc1-beck-helm
          path: charts/backend
          values: values-vpc1.yaml
          namespace: bookjjeok
        - name: vpc1-logging-helm
          path: charts/logging
          values: values.yaml
          namespace: logging
  template:
    metadata:
      name: '{{name}}'
    spec:
      project: default
      source:
        repoURL: https://github.com/ahnwan55/my-app
        targetRevision: main
        path: '{{path}}'
        helm:
          valueFiles:
          - values.yaml
          - '{{values}}'
      destination:
        name: bookjjeok-cloud-eks-cluster
        namespace: '{{namespace}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
        - CreateNamespace=true
